package views;

import haxe.ui.containers.ScrollView;

@:build(haxe.ui.ComponentBuilder.build("xml/main-view.xml"))
class MainView extends ScrollView {
    public function new() {
        super();
    }
}